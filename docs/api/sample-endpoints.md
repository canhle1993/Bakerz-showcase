# Sample Endpoints

This file is for public API examples only. Do not include private credentials or internal-only implementation details.

## Example Routes

- `GET /`
- `GET /shop_all`
- `GET /productsingle/{product}`
- `GET /filter`
- `GET /contact`
- `POST /login`
- `POST /register`

## Notes

- Keep this section high-level
- Avoid exposing sensitive business logic
- Prefer screenshots or request/response examples over real source code

